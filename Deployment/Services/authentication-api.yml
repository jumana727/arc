apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentication-api-deployment
  labels:
    app: authentication-api
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authentication-api
  template:
    metadata:
      labels:
        app: authentication-api
    spec:
      containers:
      - name: authentication-api
        image: jumanakj/arc-auth:2.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
---
apiVersion: v1
kind: Service
metadata:
  name: authentication-api-service
  labels:
    app: authentication-api
spec:
  selector:
    app: authentication-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

